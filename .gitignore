# Rust
/target
**/target

# Node / frontend
node_modules
/dist
*.tsbuildinfo

# ts-rs default per-type export dir (we use the single-file generator instead)
/bindings
**/bindings

# Tauri
/src-tauri/gen

# Editor / OS
.DS_Store
*.swp
.idea
.vscode

# Local env
.env
.env.local

# Never commit a local Popush config (real server details live here).
/config.toml
config.local.toml

# Saved SSH passwords (opt-in, plain text, per-machine). Never commit.
secrets.json
/secrets.json
